import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class PorterData extends ConsumerStatefulWidget {
  const PorterData({Key? key}) : super(key: key);

  @override
  ConsumerState<ConsumerStatefulWidget> createState() => _PorterDataState();
}

class _PorterDataState extends ConsumerState<PorterData> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
